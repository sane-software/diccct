# Swift Package Manager / build output
.build/
.swiftpm/
Package.resolved

# Assembled app bundle produced by scripts/build-app.sh
/SaneDiccct.app/
*.app/

# macOS cruft
.DS_Store

# Xcode user state (in case the package is opened in Xcode)
xcuserdata/
*.xcuserstate

# NOTE: dict.cc translation files are private-use only per their license and
# must never be committed. They live in ~/Library/Application Support/SaneDiccct/.
*.txt.dictcc
